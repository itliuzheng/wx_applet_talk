
<view class="system-page">
  <view class="content">{{item.content}}</view>
  <view class="desc">
  <!-- 点击立即查看 -->
    <view data-id="{{item.id}}" bindtap="setRead">点击立即查看</view>
    <view class="date">{{item.createTime}}</view>
  </view>
</view>